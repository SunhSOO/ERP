# 아키텍처 결정 기록(Decision Log)

- 기준선 승인일: 2026-08-05
- 승인 범위: ADR-001 ~ ADR-015

## ADR-001 — 전체 설계, 단계별 활성화

- 상태: Approved
- 결정: 전체 전사 모듈의 데이터와 연결 구조를 먼저 설계하되 실제 개발·운영은 단계별로 진행한다.
- 이유: 모듈 간 연결을 잃지 않으면서도 초기 실패 위험을 낮춘다.
- 결과: Phase 0~7 로드맵과 feature flag가 필요하다.

## ADR-002 — 모듈형 모놀리스 우선

- 상태: Approved
- 결정: 초기 백엔드는 모듈형 모놀리스로 구축한다.
- 이유: 최대 30명 규모에서는 마이크로서비스 운영 비용이 과도하다.
- 예외: AI 추론, 문서 처리, 검색, 외부 연동 워커는 자원/장애 특성상 별도 프로세스 또는 서버로 분리 가능.

## ADR-003 — 웹/PWA 우선

- 상태: Approved
- 결정: 반응형 웹과 PWA를 우선하며 네이티브 모바일 앱은 후순위로 둔다.
- 이유: 작은 조직에서 개발·배포·유지보수 비용을 줄인다.

## ADR-004 — PostgreSQL 단일 업무 진실원천

- 상태: Approved
- 결정: 관계형 업무 데이터는 PostgreSQL을 기준으로 한다.
- 보조 저장소: MinIO(파일), OpenSearch(검색), Qdrant(벡터), Redis(캐시/큐).
- 결과: 보조 저장소는 DB/원문에서 재구성 가능해야 한다.

## ADR-005 — 회계·급여 법정 처리 외부 연동

- 상태: Approved
- 결정: ERP는 비용·예산·매출·프로젝트 손익과 인사·근태 관리 장부를 제공하되 법정 회계·급여·세무 신고 엔진은 자체 구현하지 않는다.
- 이유: 법적 정확성과 변경 대응 위험.

## ADR-006 — 프로젝트 중심 연결

- 상태: Approved
- 결정: 업무, 회의, 문서, 산출물, 비용, 구매, 자산, Git 활동은 가능한 경우 프로젝트와 연결한다.
- 결과: 프로젝트 상세가 통합 업무 허브가 된다.

## ADR-007 — AI의 DB 직접 접근 금지

- 상태: Approved
- 결정: AI는 Tool Gateway를 통해 검증된 업무 API만 호출한다.
- 이유: 권한·감사·상태 규칙·idempotency를 강제하기 위함.

## ADR-008 — 중요 AI 실행의 사람 승인

- 상태: Approved
- 결정: 금전, 계약, 인사, 권한, 외부 발송, 대량 변경은 승인 후 실행한다.
- 결과: AI approval queue와 고정 payload가 필요하다.

## ADR-009 — 문서 버전 불변

- 상태: Approved
- 결정: 문서 버전은 생성 후 불변이며 승인본/제출본을 별도 포인터로 관리한다.
- 이유: 최신본 혼선과 제출 이력 훼손 방지.

## ADR-010 — Docker Compose로 시작

- 상태: Approved
- 결정: 초기 자체 서버 운영은 Docker Compose를 기준으로 한다.
- 이유: 최대 30명 규모에서 k8s/k3s는 초기 복잡도가 과함.
- 전환: 다중 노드/무중단/독립 배포 필요가 실제로 발생할 때 k3s 검토.

## ADR-011 — 외부 연동 실패 격리

- 상태: Approved
- 결정: 이메일, Git, 캘린더, 회계 연동은 비동기 큐와 재시도로 처리한다.
- 결과: 외부 장애가 핵심 ERP 트랜잭션을 롤백하지 않는다.

## ADR-012 — 상태 전이 API

- 상태: Approved
- 결정: 계약·프로젝트·결재 등 핵심 상태는 일반 PATCH가 아니라 명시적 transition API로 변경한다.
- 이유: 조건, 권한, 사유, 감사의 일관성.

## ADR-013 — 소프트 삭제/보관 기본

- 상태: Approved
- 결정: 핵심 업무 레코드는 물리 삭제 대신 취소/무효/보관 상태를 사용한다.
- 예외: 임시 업로드, 만료 세션, 정책상 폐기 가능한 개인정보 등.

## ADR-014 — 검색 결과 원본 권한 재검증

- 상태: Approved
- 결정: 검색/벡터 인덱스 필터에 더해 원본 API에서 권한을 재검증한다.
- 이유: 색인 지연이나 잘못된 ACL 메타데이터로 인한 노출 방지.

## ADR-015 — 사내 메신저 완전 대체는 후순위

- 상태: Approved
- 결정: 프로젝트 댓글·멘션·알림·활동 피드를 우선 구현한다.
- 이유: 완전한 메신저는 범위와 운영 부담이 크고 ERP 핵심 가치와 거리가 있다.

---

# 2차 목업 트랙 결정 (ADR-016 ~ ADR-019)

- 추가일: 2026-09-07
- 배경: `UI mockups/exports/`의 2차 화면 목업이 1차 ERP 목업을 폐기하고 제품 핵심을
  재정의했다. 목업 인덱스가 "1차 ERP 목업은 브리프와 정합하지 않아 폐기했습니다"라고
  명시한다.

## ADR-016 — 목업 제품 병행 트랙

- 상태: Approved
- 날짜: 2026-09-07
- 배경: 2차 목업이 정의하는 제품은 `docs/specs`의 99화면 전사 ERP가 아니다. 로컬 LLM,
  옵시디언 지식 볼트, hwpx 문서 변환, 하이웍스 메일 분류, 깃허브 ↔ WBS 정합, 발주사와
  용역사 이중 역할을 축으로 하는 프로젝트 지식·문서 자동화 도구다. CRM, 견적·계약,
  전자결재, 재무·구매, 인사·자산이 목업에 없다.
- 선택지: (1) 목업으로 ERP 설계를 대체 (2) ERP 설계를 기준으로 목업을 부분집합 취급
  (3) 두 트랙을 병행 관리
- 결정: 병행 관리한다. `docs/specs`의 ERP 설계와 71개 작업 패키지를 그대로 유지하고,
  목업 제품을 `pkd`(project knowledge and documents) 트랙으로 분리해 먼저 구현한다.
  작업 패키지 ID 공간을 `WP-PKD-*`로 분리한다.
- 이유: ERP 설계는 승인된 자산이며 폐기할 근거가 없다. 목업 제품이 먼저 필요한 것도
  사실이다. 사용자·세션·MFA(WP-PLT-002)와 Compose 기반(WP-OPS-001)처럼 제품 중립인
  플랫폼 산출물은 두 트랙이 공유해 중복을 피한다.
- 영향: `docs/work-packages/pkd_work_packages.yaml`을 신설한다. 기존
  `work_packages.yaml`은 손대지 않고 `WP-UI-001`에 교차 참조 주석만 남긴다.

### 태스크 상태값

목업은 `차단`, `진행`, `완료`, `깃허브만`을 쓰고 ERP 설계는 다섯 단계 흐름을 쓴다.
`pkd` 트랙은 목업 값을 정본으로 삼는다. 두 트랙을 합칠 때 쓸 매핑은 다음과 같다.

| pkd 트랙 | ERP 트랙 | 비고 |
|---|---|---|
| `PLANNED` 예정 | `BACKLOG`, `TODO` | 착수 전 |
| `IN_PROGRESS` 진행 | `IN_PROGRESS` | |
| `BLOCKED` 차단 | `BLOCKED` | 차단 사유와 해소 담당자 필요 |
| `DONE` 완료 | `REVIEW`, `DONE` | pkd는 검토 단계를 별도로 두지 않는다 |
| `VCS_ONLY` 깃허브만 | 대응 없음 | WBS에 정의되지 않았으나 저장소에서 감지된 작업 |

`VCS_ONLY`는 ERP 설계에 대응 상태가 없다. 합칠 때 별도 결정이 필요하다.

## ADR-017 — 프론트엔드 스타일링은 Tailwind CSS v4

- 상태: Approved
- 날짜: 2026-09-07
- 배경: 저장소에 스타일링 방식을 정한 결정이 없었다. `apps/web`은 CSS 프레임워크 없이
  24줄짜리 전역 CSS만 가지고 있다. 목업은 CSS 사용자 정의 속성과 유틸리티 클래스로
  손으로 짠 "Industry" 디자인 시스템을 쓴다.
- 선택지: (1) 목업 CSS를 그대로 쓰고 CSS Modules로 조합 (2) Tailwind CSS v4
  (3) vanilla-extract
- 결정: Tailwind CSS v4를 쓴다. 목업의 토큰을 `@theme` 블록으로 옮기고, 목업의 컴포넌트
  클래스는 `@layer components`에 그대로 이식한다. Tailwind 유틸리티는 새 조합에만 쓴다.
- 이유: 설정 파일 없이 CSS만으로 토큰을 정의하는 v4 방식이 목업의 사용자 정의 속성
  구조와 그대로 맞는다. 컴포넌트 클래스를 이식하는 절충으로 시각 편차 위험을 줄인다.
- 영향: 목업의 간격 스케일은 3.4px의 배수다. Tailwind v4의 동적 간격 기준값
  `--spacing`을 `3.4px`로 두면 `p-1 p-2 p-3 p-4 p-6 p-8`이 목업의
  3.4 / 6.8 / 10.2 / 13.6 / 20.4 / 27.2px과 정확히 일치한다. 별도 간격 토큰이나
  사용 금지 규칙이 필요 없다.

## ADR-018 — 통합은 포트와 어댑터로 구현한다

- 상태: Approved
- 날짜: 2026-09-07
- 배경: 목업이 요구하는 외부 연동 다섯 가지 중 hwpx 변환기(kordoc)만 존재한다.
  옵시디언 볼트 경로, 깃허브 토큰, 하이웍스 계정, 사내 GPU 추론 서버가 아직 없다.
- 결정: 도메인 계층에 포트를 정의하고 인프라 계층에 픽스처 어댑터와 실제 어댑터를 둔다.
  어댑터 선택은 환경 변수로 하고 기본값은 픽스처다.
- 이유: 자원이 준비되기를 기다리지 않고 제품 전체를 완성할 수 있다. 화면과 도메인
  로직이 어댑터 교체와 무관하게 남는다. 테스트가 외부 서비스를 호출하지 않는다는
  `AGENTS.md` 14절 규칙도 함께 지켜진다.
- 영향: 포트 다섯 개를 정의한다. `DocumentConverterPort`, `VcsPort`, `VaultPort`,
  `LlmPort`, `MailPort`. `LlmPort`의 실제 어댑터는 OpenAI 호환 규격으로 짜서 ollama와
  vLLM 양쪽에 붙을 수 있게 한다.

## ADR-019 — 접근성 우선 토큰 보정

- 상태: Approved
- 날짜: 2026-09-07
- 배경: 목업 토큰 네 항목이 배경 `#f2f2f3` 기준 WCAG AA 4.5:1에 미달한다.
- 결정: 아래와 같이 보정한다. 목업과 미세하게 달라지는 것을 감수한다.

| 대상 | 측정값 | 조치 |
|---|---|---|
| `neutral-500` `#98989b` 보조 텍스트 | 2.6:1 | 텍스트 용도 금지. 테두리·장식 전용 |
| `.text-muted` 잉크 55% | 3.7:1 | `--color-text-muted`를 신설해 `neutral-700`에 연결 |
| `neutral-600` `#7a7a7d` 보조 텍스트 | 3.9:1 | `neutral-700`으로 대체 |
| `.btn-primary` 배경 `accent` `#5980a6` | 4.1:1 | 배경을 `accent-700` `#416180`로 변경 |

- 이유: `neutral-700`은 5.9:1, `accent-700`은 6.5:1로 통과한다.
  `06_UI_UX_INFORMATION_ARCHITECTURE.md` 13절이 색상 대비 검증을 요구한다.
- 영향: 목업과 픽셀 동일은 불가능하다. 구조와 토큰 수준의 일치를 충실도 기준으로 삼는다.

---

## 결정 추가 템플릿

```text
## ADR-XXX — 제목

- 상태: Proposed / Approved / Superseded / Rejected
- 날짜:
- 결정자:
- 배경:
- 선택지:
- 결정:
- 이유:
- 영향:
- 대체/후속 결정:
```

---
